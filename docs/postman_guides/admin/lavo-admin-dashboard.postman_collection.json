{
  "info": {
    "name": "Hurryline - Admin Dashboard API (360 Matrix)",
    "description": "Admin dashboard and analytics with matrix.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "access_token",
      "value": "",
      "type": "string"
    },
    {
      "key": "base_url",
      "value": "http://localhost:3000",
      "type": "string"
    },
    {
      "key": "cron_secret",
      "value": "",
      "type": "string"
    },
    {
      "key": "metric",
      "value": "",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "Admin Dashboard & Analytics",
      "item": [
        {
          "name": "/v1/admin/analytics/{{metric}}",
          "item": [
            {
              "name": "GET /v1/admin/analytics/{{metric}} - Success",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has envelope', () => { pm.expect(j).to.be.an('object'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/analytics/{{metric}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "analytics",
                    "{{metric}}"
                  ]
                },
                "description": "GET /api/v1/admin/analytics/{{metric}} (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/analytics/{{metric}} - Unauthorized (401)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 401', () => pm.response.to.have.status(401));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/analytics/{{metric}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "analytics",
                    "{{metric}}"
                  ]
                },
                "description": "GET /api/v1/admin/analytics/{{metric}} (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/analytics/{{metric}} - Validation (400)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 400', () => pm.response.to.have.status(400));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/analytics/{{metric}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "analytics",
                    "{{metric}}"
                  ]
                },
                "description": "GET /api/v1/admin/analytics/{{metric}} (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/analytics/{{metric}} - Invalid Path / Not Found",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 400', () => pm.response.to.have.status(400));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/analytics/invalid-id",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "analytics",
                    "invalid-id"
                  ]
                },
                "description": "GET /api/v1/admin/analytics/{{metric}} (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/analytics/{{metric}} - Forbidden (403)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 403', () => pm.response.to.have.status(403));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/analytics/{{metric}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "analytics",
                    "{{metric}}"
                  ]
                },
                "description": "GET /api/v1/admin/analytics/{{metric}} (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/analytics/{{metric}} - Server Error Contract (500)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 500', () => pm.response.to.have.status(500));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/analytics/{{metric}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "analytics",
                    "{{metric}}"
                  ]
                },
                "description": "GET /api/v1/admin/analytics/{{metric}} (code-derived matrix)"
              },
              "response": []
            }
          ]
        },
        {
          "name": "/v1/admin/dashboard",
          "item": [
            {
              "name": "GET /v1/admin/dashboard - Success",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 200', () => pm.response.to.have.status(200));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has envelope', () => { pm.expect(j).to.be.an('object'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/dashboard",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "dashboard"
                  ]
                },
                "description": "GET /api/v1/admin/dashboard (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/dashboard - Unauthorized (401)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 401', () => pm.response.to.have.status(401));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/dashboard",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "dashboard"
                  ]
                },
                "description": "GET /api/v1/admin/dashboard (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/dashboard - Validation (400)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 400', () => pm.response.to.have.status(400));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/dashboard",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "dashboard"
                  ]
                },
                "description": "GET /api/v1/admin/dashboard (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/dashboard - Forbidden (403)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 403', () => pm.response.to.have.status(403));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/dashboard",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "dashboard"
                  ]
                },
                "description": "GET /api/v1/admin/dashboard (code-derived matrix)"
              },
              "response": []
            },
            {
              "name": "GET /v1/admin/dashboard - Server Error Contract (500)",
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status 500', () => pm.response.to.have.status(500));",
                      "pm.test('JSON response', () => { pm.response.to.be.json; });",
                      "const j = pm.response.json(); pm.test('Has error code/message', () => { pm.expect(j).to.have.property('message'); pm.expect(j).to.have.property('code'); });"
                    ]
                  }
                }
              ],
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{access_token}}"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/api/v1/admin/dashboard",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "api",
                    "v1",
                    "admin",
                    "dashboard"
                  ]
                },
                "description": "GET /api/v1/admin/dashboard (code-derived matrix)"
              },
              "response": []
            }
          ]
        }
      ]
    }
  ]
}
